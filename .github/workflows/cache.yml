on:
  push: {}
  workflow_dispatch: {}

jobs:
  add_cache:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          echo "abc" > ./README.md
      - uses: actions/cache/save@v4
        id: cache
        with:
          path: ./README.md
          key: custom-key
              
